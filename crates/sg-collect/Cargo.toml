[package]
name = "sg-collect"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Built-in ServerGlass collectors: agentless Linux metrics from /proc and /sys."

[dependencies]
sg-model = { workspace = true }
